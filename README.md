🖼️ Simple OpenCV Image Processor

This is a beginner-friendly Python + OpenCV project that allows users to load an image, convert it to grayscale, and either display or save the processed image based on user input.

🚀 Features

Load an image using a full file path

Convert the image to grayscale

User choice to:

📸 Show the image

💾 Save the grayscale image

Basic error handling for invalid image paths

🛠️ Technologies Used

Python 🐍

OpenCV (cv2)
